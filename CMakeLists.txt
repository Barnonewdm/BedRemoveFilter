PROJECT(BedRemovalFilter)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

find_package(ITK REQUIRED)
include(${ITK_USE_FILE})


ADD_EXECUTABLE(BedRemovalFilter main.cxx)
TARGET_LINK_LIBRARIES(BedRemovalFilter ${ITK_LIBRARIES})
